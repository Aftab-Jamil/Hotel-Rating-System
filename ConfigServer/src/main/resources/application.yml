server:
  port: 8084
spring:
  application:
    name: ConfigServer
  cloud:
    config:
      server:
        git:
          uri: https://github.com/Aftab-Jamil/config-server
          clone-on-start: true
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8000/eureka/

